{
  "name": "nofins",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "bin/bridgetown frontend:build && bin/bridgetown start",
    "esbuild": "node esbuild.config.js --minify",
    "esbuild-dev": "node esbuild.config.js --watch",
    "deploy": "rm -rf output/* && BRIDGETOWN_ENV=production bin/bridgetown frontend:build && BRIDGETOWN_ENV=production bin/bridgetown build && cp .nojekyll output/. && cp CNAME output/. && cd output && git add . && git commit -m build && git push"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.0",
    "esbuild": "^0.28.0",
    "glob": "^13.0.6",
    "postcss": "^8.5.15",
    "postcss-import": "^16.1.1",
    "postcss-load-config": "^6.0.1",
    "tailwindcss": "^4.3.0"
  }
}
